# VITE_API_URL is intentionally NOT set here.
# Vercel rewrites /api/* -> https://viralstan-system-crm.onrender.com/api/*
# So the app uses '/api' fallback (defined in api.js) and Vercel proxies it server-side — no CORS needed.
